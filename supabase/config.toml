# https://supabase.com/docs/guides/functions
# Webhook must receive unsigned Stripe requests (no Supabase JWT).

[functions.stripe-webhook]
verify_jwt = false

# Public Stripe return redirect → app deep link (no Authorization header).
[functions.subscription-return]
verify_jwt = false
